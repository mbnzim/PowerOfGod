@model IEnumerable<PowerOfGod.Domain.Entity.Booking.TypeOFVenue>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutLess.cshtml";
}
<br /><br /><br /><br /><br />
<h2>List Of View Types</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VenueType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VenueType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VenueCodeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VenueCodeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VenueCodeID })
        </td>
    </tr>
}

</table>
