@model IEnumerable<PowerOfGod.Domain.Entity.Booking.TypeOfPastor>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutLess.cshtml";
}
<br /><br /><br /><br /><br />
<h2>List Of Paster Types</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pastortype)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pastortype)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TypeOfPastorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TypeOfPastorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TypeOfPastorID })
        </td>
    </tr>
}

</table>
