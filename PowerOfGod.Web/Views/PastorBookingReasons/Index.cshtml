@model IEnumerable<PowerOfGod.Domain.Entity.Booking.PastorBookingReason>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutLess.cshtml";
}
<br /><br /><br /><br /><br />
<h2>Booking Reasons</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Reason)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Reason)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReasonID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReasonID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReasonID })
        </td>
    </tr>
}

</table>
